# Environment Variables for DCMS Website
# Copy this file to .env.local for local development
# For production, set these variables in your deployment platform (Netlify, Vercel, etc.)

# Node Environment
# Options: development, production, test
# Default: development (automatically set by Next.js)
NODE_ENV=production

# Next.js Configuration
# These are automatically handled by Next.js, but can be overridden if needed
# NEXT_PUBLIC_* variables are exposed to the browser

# Optional: Analytics and Tracking
# Uncomment and add your keys if you want to integrate analytics
# NEXT_PUBLIC_GA_ID=your-google-analytics-id
# NEXT_PUBLIC_GTM_ID=your-google-tag-manager-id

# Optional: External API Keys
# If you need to integrate with external services, add them here
# NEXT_PUBLIC_API_URL=https://api.example.com
# API_SECRET_KEY=your-secret-key-here

# Optional: Image Optimization
# Next.js Image Optimization domain (already configured in next.config.js)
# IMAGE_DOMAIN=dcms.ac.in

# Optional: Video Configuration
# If you need to change video source or add CDN
# NEXT_PUBLIC_VIDEO_CDN_URL=https://cdn.example.com

# Notes:
# - Variables prefixed with NEXT_PUBLIC_ are exposed to the browser
# - Never commit .env.local or .env files to version control
# - Always use .env.example as a template
# - For production, set variables in your hosting platform's environment settings
