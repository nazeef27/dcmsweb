"use client";

import { useEffect } from "react";

export function SmoothScroll({ children }: { children: React.ReactNode }) {
  useEffect(() => {
    // Smooth scroll behavior is handled by CSS
    // Additional smooth scroll enhancements can be added here
  }, []);

  return <>{children}</>;
}



